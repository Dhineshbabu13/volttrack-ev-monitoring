import { useEffect, useState } from 'react'; function VehicleTest() { const [vehicles, setVehicles] = useState([]); useEffect(() => { fetch('http://localhost:4000/api/vehicles') .then((res) => res.json()) .then((data) => setVehicles(data)); }, []); return ( <div style={{ padding: '40px' }}> <h1>My Real EV Fleet ({vehicles.length} vehicles)</h1> <ul> {vehicles.map((v) => ( <li key={v.vehicle_id}> {v.vehicle_id} — {v.vehicle_brand} {v.vehicle_model} — Range: {v.range_km} km — Battery: {v.battery_capacity_kwh} kWh </li> ))} </ul> </div> ); } export default VehicleTest;